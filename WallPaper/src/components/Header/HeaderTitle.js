import React from "react";
import Typography from "../Typography";

const HeaderTitle = (props) => {
  return <Typography size={18}>{props.title}</Typography>;
};

export default HeaderTitle;
